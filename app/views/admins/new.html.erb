<div class = 'container admin-new'>
  <div class="row">
    <div class="category-title col-11 offset-1">
      <h1 >カテゴリ登録</h1>
        <h2 >現在存在するカテゴリー</h2>
    </div>
          <div class = 'col-5 offset-1'>
            <h2>デッキタイプ</h2>
              <ul class="tab_menu">
                <% @deck_themes.each do |f| %>
                  <li><%= f.deck_theme %></li>
                <% end %>
              </ul>
            <%= link_to '追加', add_deck_theme_path %>
          </div>
      
          <div class = 'col-6'>
            <h2>リンクステーマ</h2>
              <ul class="tab_menu">
                <% @link_themes.each do |f| %>
                  <li><%= f.link_theme %></li>
                <% end %>
              </ul>
            <%= link_to '追加', add_link_theme_path %>
          </div>
  </div>
</div>